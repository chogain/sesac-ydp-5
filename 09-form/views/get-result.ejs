<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GET 요청 Form 전송 결과</title>

    <style>
      span {
        color: blue;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <h1>GET 요청 폼 결과 확인</h1>
    <div><span><%=userInfo.id %></span>님 환영합니다.</div>
    <div>
      <span><%= userInfo.id %></span>님의 비밀번호는
      <span><%= userInfo.pw %></span>입니다.
    </div>
    <div>
      <span><%= userInfo.id %></span>님의 Email은
      <span><%= userInfo.email %></span>입니다.
    </div>
    <% if (userInfo.grade) {%>
    <div>
      <span><%= userInfo.id %></span>님의 학년은
      <span><%= userInfo.grade %></span>학년 입니다.
    </div>
    <% }%>
  </body>
</html>
