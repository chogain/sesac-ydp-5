<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form 전송 결과</title>

    <style>
      span {
        color: blue;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <h1>실습 GET 요청 폼 결과 확인</h1>
    <div><span><%=userInfo.name %></span>님 환영합니다.</div>
    <div>
      <span><%=userInfo.name %></span>님의 성별은
      <span><%=userInfo.gender %></span>입니다.
    </div>
    <div>
      <span><%=userInfo.name %></span>님의 생년월일은
      <span><%=userInfo.year %></span>년 <span><%=userInfo.month %></span>월
      <span><%=userInfo.day %></span>일 입니다.
    </div>
    <div>
      <span><%=userInfo.name %></span>님의 관심사는
      <span><%=userInfo.like %></span>입니다.
    </div>
    <div>입력하신 정보가 맞다면 확인 버튼을 눌러주세요.</div>
    <button>확인</button>
    <br />
    <a href="/">홈으로 이동</a>
  </body>
</html>
