<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form 전송 결과</title>

    <style>
      span {
        color: blueviolet;
        font-weight: 700;
      }
    </style>
  </head>
  <body>
    <h1><%= title %>요청 폼 결과 확인</h1>
    <div><span><%=userInfo.id %></span>님 환영합니다.</div>
    <div>
      <span><%= userInfo.id %></span>님의 비밀번호는
      <span><%= userInfo.pw %></span>입니다.
    </div>
    <br />
    <a href="/">홈으로 이동</a>
  </body>
</html>
